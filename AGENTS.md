# AGENTS.md

## 工作边界
1. 不破坏现有可用主链路（发送/停止/CAD执行安全门）
2. 新能力采用可回退开关
3. 每次改动必须可验证

## 执行原则
- 先读现状，再改代码。
- 小步提交：每步只做一类改动。
- 改后自检：确保无明显错误。

## 多能力协同策略
- 先由大模型理解用户意图（默认）
- 当命中工具能力时再触发工具调用（KB/ERP/File）
- 工具结果返回后再由大模型组织最终回答

## 失败兜底
- 工具失败时，向用户解释失败原因并给下一步建议
- 不因单一工具失败阻断整体会话
